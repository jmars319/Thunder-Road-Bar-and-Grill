# Thunder Road Project Notes

## Deployment
Deployed on GoDaddy cPanel (forced to use PHP backend instead of Node).

## Structure
- `/frontend/` - React app (active)
- `/backend/` - PHP backend deployed to GoDaddy (`public_html/api/`)

## Status
Deployed and live. No active development beyond maintenance.
